extends Node3D

func _process(delta: float) -> void:
	pass
	#var image_texture: ImageTexture = ImageTexture.new()
	#$SpotLight3D.light_projector = image_texture.create_from_image($MeshInstance3D2/SubViewport.get_texture())
